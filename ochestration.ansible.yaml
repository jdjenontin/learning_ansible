---
  - name: "Ochestration test"
    hosts: logicservers
    serial: 1

    tasks:

    - name: "Step 1"
      debug:
        msg: "Running Step 1 on {{ inventory_hostname }}"

    - name: "Step 2"
      debug:
        msg: "Running Step 2 on {{ inventory_hostname }}"